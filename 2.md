# Linux-CLI-Note

本项目用于记录在 Linux (Debian/Armbian) 环境下部署服务、容器管理、网络配置以及性能优化的常用命令行。作为个人的高频 CLI 速记本，本仓库特别针对 Homelab 玩家、旁路由网关配置、代理环境调试以及 Docker 容器化部署进行了梳理与总结。

## 📂 目录导航
- 🐧 [1. Debian/Armbian 系统核心调优](#1-debianarmbian-系统核心调优)
- 🐳 [2. Docker & Compose 高效运维](#2-docker--compose-高效运维)
- 🌐 [3. 网络配置与旁路由网关优化](#3-网络配置与旁路由网关优化)
- 🚀 [4. 硬件性能调试与系统监控](#4-硬件性能调试与系统监控)
- 🛠️ [5. 常用 Shell 脚本与自动化运维](#5-常用-shell-脚本与自动化运维)


## 🐧 1. Debian/Armbian 系统核心调优

### 系统更新与清理
```bash
# 更新软件包列表并升级系统
apt update && apt upgrade -y

# 自动移除不需要的依赖包并清理缓存
apt autoremove -y && apt clean

```

### 时间与时区设置

```bash
# 修改系统时区（如设置为 Asia/Shanghai）
dpkg-reconfigure tzdata

# 手动同步网络时间
ntpdate -u pool.ntp.org

```

### 服务管理 (Systemd)

```bash
# 重载 systemd 守护进程（修改 service 文件后必须执行）
systemctl daemon-reload

# 设置服务开机自启并立即启动 (例如 ssh)
systemctl enable --now ssh

# 查看服务运行状态
systemctl status <service_name>

```

---

## 🐳 2. Docker & Compose 高效运维

### 安装与基础环境

```bash
# 官方一键安装脚本
curl -fsSL [https://get.docker.com](https://get.docker.com) -o get-docker.sh
sh get-docker.sh

# 将当前用户加入 docker 组（免 sudo 运行）
usermod -aG docker $USER

```

### 高频容器操作

```bash
# 实时查看容器日志（尾部100行）
docker logs -f --tail 100 <container_name>

# 进入正在运行的容器内部交互终端
docker exec -it <container_name> /bin/sh

# 查看所有容器的 CPU/内存 占用状态
docker stats

```

### Docker Compose

```bash
# 后台启动所有服务
docker-compose up -d

# 停止并移除容器、网络、挂载卷
docker-compose down -v

# 重新拉取镜像并重建容器
docker-compose up -d --build --force-recreate

```

### 空间清理

```bash
# 【危险操作】清理所有未使用的镜像、容器、网络和无名卷
docker system prune -a --volumes

```

---

## 🌐 3. 网络配置与旁路由网关优化

在部署 sing-box、Xray-core 或 OpenWrt 旁路由时，经常需要调试网络和路由表。

### IP 与端口排查

```bash
# 查看本机所有网卡及 IP 地址
ip a

# 查看监听端口（定位端口冲突非常有用）
ss -tulpn | grep LISTEN
# 或者使用 netstat
netstat -tulnp

```

### 路由表与网关控制

```bash
# 查看当前系统路由表
ip route show

# 临时修改默认网关（适用于旁路由透明代理测试）
ip route add default via 192.168.1.x

# 删除旧的默认网关
ip route del default

```

### DNS 测试与调试

```bash
# 测试域名解析
nslookup github.com

# 强制刷新/重启 DNS 解析服务
systemctl restart systemd-resolved

```

---

## 🚀 4. 硬件性能调试与系统监控

针对 ARM 开发板（如玩客云、树莓派等）和 VPS 的常用状态监控。

### 系统资源与存储

```bash
# 动态资源监控（比 top 更直观）
htop

# 查看内存与 Swap 使用情况
free -h

# 查看磁盘空间占用
df -h

```

### ARM 硬件温度与监控

```bash
# 查看 CPU 温度 (Armbian 常用路径)
cat /sys/class/thermal/thermal_zone0/temp | awk '{print $1/1000 "°C"}'

# 查看 CPU 当前频率
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq

```

---

## 🛠️ 5. 常用 Shell 脚本与自动化运维

### 脚本权限与执行

```bash
# 赋予脚本可执行权限
chmod +x echnet.sh

# 运行当前目录下的脚本
./echnet.sh

```

### 定时任务 (Cron)

```bash
# 编辑当前用户的定时任务
crontab -e

# 示例：每天凌晨 3 点执行网关切换脚本，并将日志输出到 /var/log
0 3 * * * /root/echnet.sh >> /var/log/echnet.log 2>&1

```

### 内网穿透与后台守护 (Cloudflare Tunnel 等)

```bash
# 使用 nohup 后台运行程序并忽略挂断信号
nohup ./cloudflared tunnel run <token> > cloudflared.log 2>&1 &

# 查看后台任务
jobs -l

```

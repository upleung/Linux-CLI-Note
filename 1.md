# 🐧 Linux-CLI-Note  
**Debian / Armbian / Docker / Linux 高频命令行笔记库**  
记录我在 Linux (Debian / Armbian) 环境下的系统调优、Docker 运维、硬件调试、自动化脚本等常用命令与最佳实践。

适用于：  
- Armbian / Debian / Ubuntu  
- Docker / Compose 运维  
- ARM 设备（玩客云、R2S、R4S、树莓派等）  
- IPTV / Streamlink / FFmpeg / 网络调试  
- WSL2 / Cloudflare Pages / Workers 部署  
- 日常 Linux CLI 高频命令速查

---

## 📂 目录导航
- 🐧 [1. Debian / Armbian 系统核心调优](#1-debian--armbian-系统核心调优)
- 🐳 [2. Docker & Compose 高效运维](#2-docker--compose-高效运维)
- 🚀 [3. 硬件性能调试与优化](#3-硬件性能调试与优化)
- 🛠️ [4. Shell 自动化脚本与工具集](#4-shell-自动化脚本与工具集)
- 🌐 [5. 网络调试 / IPTV / Streamlink](#5-网络调试--iptv--streamlink)
- 🧰 [6. WSL2 / Cloudflare / 部署笔记](#6-wsl2--cloudflare--部署笔记)
- 📦 [7. Git / GitHub / 版本管理](#7-git--github--版本管理)
- 📘 [8. 常用 Linux CLI 速查表](#8-常用-linux-cli-速查表)

---

# 🐧 1. Debian / Armbian 系统核心调优

### 🔧 基础更新与常用工具
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y vim htop curl wget git net-tools lsof unzip
```

### 🔥 CPU 性能模式（Armbian）
```bash
armbian-config
# System → CPU → Performance
```

### 📈 查看系统硬件信息
```bash
uname -a
lsb_release -a
cat /etc/os-release
lscpu
lsblk
free -h
df -h
```

### 🧹 清理系统垃圾
```bash
sudo apt autoremove -y
sudo apt autoclean
sudo journalctl --vacuum-size=100M
```

### 🧱 防止 SD 卡 / eMMC 过度写入
```bash
sudo nano /etc/fstab
# 添加：
tmpfs /tmp tmpfs defaults,noatime,nosuid,size=256m 0 0
```

---

# 🐳 2. Docker & Compose 高效运维

### 📦 安装 Docker（Debian）
```bash
curl -fsSL https://get.docker.com | bash
sudo systemctl enable docker
```

### 🧱 Docker 常用命令
```bash
docker ps -a
docker images
docker logs -f <container>
docker exec -it <container> /bin/bash
docker stop <container>
docker rm <container>
docker rmi <image>
```

### 🧰 Docker Compose 常用命令
```bash
docker compose up -d
docker compose down
docker compose logs -f
docker compose pull
```

### 🧼 清理 Docker 垃圾
```bash
docker system prune -a
docker volume prune
docker network prune
```

### 🐳 查看容器资源占用
```bash
docker stats
```

---

# 🚀 3. 硬件性能调试与优化

### 🔥 CPU 温度
```bash
cat /sys/class/thermal/thermal_zone0/temp
```

### 📊 实时监控
```bash
htop
iotop
iftop
```

### 🧪 磁盘测速
```bash
sudo hdparm -Tt /dev/sda
```

### 🧪 网络测速
```bash
speedtest-cli
```

---

# 🛠️ 4. Shell 自动化脚本与工具集

### 📜 自动重启 Docker 容器
```bash
#!/bin/bash
docker restart myapp
```

### 📜 自动备份目录到 GitHub
```bash
#!/bin/bash
cd /root/myrepo
git add .
git commit -m "auto backup $(date)"
git push
```

### 📜 自动抓取 IPTV 源（Streamlink）
```bash
streamlink --stream-url <直播地址>
```

---

# 🌐 5. 网络调试 / IPTV / Streamlink

### 🔍 查看端口占用
```bash
sudo lsof -i :8080
sudo netstat -tunlp
```

### 📡 测试直播源是否可用
```bash
ffprobe <url>
```

### 📺 Streamlink 获取真实流地址
```bash
streamlink --stream-url <直播页面>
```

### 🧪 FFmpeg 推流测试
```bash
ffmpeg -re -i input.mp4 -c copy -f flv rtmp://server/live/test
```

---

# 🧰 6. WSL2 / Cloudflare / 部署笔记

### 🐧 WSL2 设置代理（v2rayN）
```bash
export http_proxy=http://127.0.0.1:7890
export https_proxy=http://127.0.0.1:7890
```

### 🌐 Cloudflare Pages 部署前端
```bash
npm run build
wrangler pages deploy dist
```

### ⚡ Cloudflare Workers 代理 API
```bash
export default {
  async fetch(request) {
    return fetch(request.url)
  }
}
```

---

# 📦 7. Git / GitHub / 版本管理

### 🔑 生成 SSH Key
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

### 📤 推送代码
```bash
git add .
git commit -m "update"
git push
```

### 🧱 Git 常用
```bash
git status
git log --oneline
git reset --hard HEAD~1
```

---

# 📘 8. 常用 Linux CLI 速查表

### 📁 文件操作
```bash
cp a b
mv a b
rm -rf dir
mkdir dir
```

### 🔍 查找
```bash
grep -rn "keyword" .
find / -name "*.log"
```

### 📦 压缩
```bash
tar -czvf a.tar.gz dir
tar -xzvf a.tar.gz
```

### 🔐 权限
```bash
chmod 755 file
chown user:user file
```

### 🌐 网络
```bash
curl -I https://example.com
ping -c 4 8.8.8.8
```
